# 交接：這個 repo 現在做到哪、下一步做什麼

寫於 2026-09-15。接手的人先讀這一份，再讀 `README.md`。

## 先講範圍，這一段不准縮

這套東西管的是**每一份會被人讀到的中文**，不是只有跟 Vin 對話。
`copy-rules/情境目錄.json` 那 13 種情境就是範圍：計畫案、資料分析報告、進度彙整、
決策簡報、開 issue、回 issue、旅客端 UI、後台 UI、對外電子郵件、規格文件、
commit 與 PR、直接回 Vin、寫進線上試算表或線上文件的工作紀錄。
**少掉任何一種，就是把範圍縮了。**

這一條要寫在最前面，是因為 2026-09-15 真的縮過一次：那天寫語意判官的規格時，
範圍被收回「他問一句、我回一段」那一側，文件與 UI 還被寫進「這一版不做的」。
原因是接手的那個 session 只讀了這份 `HANDOFF.md` 與 `README.md`，
而那兩份當時通篇在講語意判官準不準，一個字都沒提範圍。

## 一句話的現況

兩層檢查都跑得動，第一層（掃詞）已經在擋人，**第二層（讀意思的語意判官）準確度不夠，
所以它只提醒不擋人**。接下來要做的第一件事就是把第二層弄準。

2026-09-15 補了涵蓋範圍：掃詞從名單制改成預設全掃，13 種情境逐一實測過，
測試程式是 `scratchpad/check12.py`（不在版控裡，改完重跑一次比對）。
細節看 `CHANGELOG.md` 最上面那一則。

---

## 一、已經做完的

### 語料

| 做了什麼 | 結果 |
|---|---|
| 重建語料，並且擋掉三種污染 | `build_vin_corpus.py`，41.9 萬字 |
| 從 Vin 自己的文件抽正式語體 | `extract_docs.py`，63 份文件、31.8 萬字 |
| 找出「AI 常用、他從來沒用過」的候選詞 | `mine_candidates.py` |

三種污染是：他退稿時把 AI 的爛句子一起打出來、他把 AI 的草稿整段貼回去、
檢查程式自己的回報被當成他的訊息存下來。清理之前，40 個他親口退過的詞裡有 20 個
被誤判成「他自己也用過」，清乾淨之後剩 14 個。

正式語體那一份非補不可，因為日常對話只教得會口語。補進來之前只有 6,455 字，
補進來之後是 31.8 萬字。

### 第一層：掃詞

`hooks/coined_word_guard.py`，掛在每一個工具之前（掛勾的比對規則是 `.*`）。
改檔案、`gh` 留言與開單、`git commit`、寄信、寫進線上試算表、把事情交辦出去、回話，
全部會經過它。讀檔、搜尋、跑這個 repo 自己的檢查程式不掃。

涵蓋率統計過：**它抓得到 Vin 實際退稿句子的 25.4%，誤擋率 0%。**
量的方法在 `eval.py`，回歸用的 165 句在 `eval-set.jsonl`（不在版控裡）。
25.4% 看起來低，但是它跟抱怨統計一致 —— 用詞問題本來就只佔他抱怨的 3%。

### 情境目錄

`copy-rules/情境目錄.json`，13 種情境，合計 131 條「要做到」、113 條「不准寫」、
134 條「送出前檢查」。每一條都附出處，出處是 Vin 自己講過的話，或是既有規則的標題。
另外有 27 條標成 `questioned`，那些是出處存疑但先留著的，還沒處理。

`copy-rules/scenario.py` 負責路由：依照這次要改的檔案路徑與工具，只印出那一種情境的卡片。

### 第二層：語意判官與它的考試

語意判官讀「他問的那一句」加上回覆，給 0 到 10 分。
**最重要的一個發現是：判斷一段回覆好不好，必須連同他問的那一句一起看。
只看回覆本身去評分，評出來的結果是反的。**

考試的設計在 `build_gold.py` 與 `judge_exam.py`：

- 考題兩邊都是 AI 寫的稿：壞的那幾則他當下說了聽不懂，好的那幾則他當下說了寫得好。
  他沒說話的那幾千則標成「不知道」，不進考試。
  第一版拿他自己打的訊息當好的那一邊，結果語意判官學會的是分辨誰寫的，不是分辨好不好。
- 門檻不由語意判官自己決定，由考題決定：掃過 1 到 10 分，取抓到率減誤判率最高的那一個。
- 成績寫進 `judge-score.json`，`pass` 這個欄位決定它准不准擋人。

### 抱怨學習迴圈

`hooks/complaint_learn.py` 掛在 UserPromptSubmit。Vin 一說「聽不懂」，
它就分析上一輪的問答，分成四種原因：沒回答到重點、太長、太專業、用了他沒學過的代號。
四種各自對應一個修正動作，同時寫進 `complaints.jsonl` 當成下一輪的考題。

---

## 二、現在卡在哪（接手第一件要做的事）

**語意判官的準確度。最近一次考試抓到 55%、誤判 27%，門檻 8 分。及格線是抓到 70%、誤判 15%。**

### 已知的事實，接手的人不用重查

1. **曾經考到 70%／10%，但是那一版的評分規範沒有被保留下來。**
   `judge-rubric.best.txt` 這個檔名看起來像備份，實際內容跟 `judge-rubric.v0.txt`
   一模一樣，是更早的版本。所以這件事是重做，不是還原。

2. **退步的軌跡是 70／10 → 100／60 → 92／92 → 82／64 → 55／27**，
   每一次退步都追得到單一原因：加了「太長就加分」的規則太粗糙、
   「主詞不是人就加分」的規則涵蓋面太寬、把兩個子項目改成強制加分。
   但是把它們一條一條拿掉之後，也回不到 70％／10％。

3. **離線校準過一次**：窄版的「主詞不是人」規則，在被抱怨的樣本裡佔 3.2%、
   在沒被抱怨的樣本裡佔 0.3%。這個差距只夠拿來當加分用，不能當成通則。

4. **語意判官早期會把 Vin 的口語改成公文。** 有一版把他親手寫的六個句子全部退掉，
   改寫成更書面、更多成語的版本。現在的規範裡明講「把他的句子改成書面語是錯的，
   不是改進」，情況有改善，但是不穩定。

### 建議的下一步順序

**先解決量測，再解決語意判官。** 因為現在的考題只有 11 對 11，一個樣本就等於 9 個百分點，
所以 70% 跟 55% 之間到底是真的退步還是抽樣造成的，量不出來。調一個量不準的東西，
每一次改都是在追雜訊。

1. 把考題規模拉大，並且做重複測量與信賴區間。
2. 考題規模夠了之後，一次只改評分規範的一條，每改一條就重考一次。
3. 改對的那一版立刻另存一份，而且要用內容比對確認存進去的真的是那一版
   （這次就是敗在沒有比對）。

---

## 三、後續要做的（排在語意判官後面）

| 事情 | 為什麼還沒做 |
|---|---|
| 他認可過的段落只有 10 段 | 誤判率量不準。這一種只有 Vin 說「這段寫得好」才會多一段，已經有掛勾在自動收 |
| 文件那一側沒有驗證過 | 一份報告或規格沒有「他問的那一句」。目前是從情境目錄取讀者會問的問題帶進去，但是那份清單是人工寫的，還沒驗證過 |
| 情境目錄的 27 條 `questioned` | 出處存疑，先留著沒刪，要逐條確認 |
| 第一層只涵蓋 25.4% | 要靠找出第 41 個詞往上推，但是那份候選名單要 Vin 本人掃過才算數 |

---

## 四、接手之前要知道的三件事

1. **這個 repo 是公開的。** 提交之前一定要掃過：同事名字、內部規則編號、
   客戶名稱、真實的報告數字、本機絕對路徑、公司信箱。做法寫在 `CLAUDE.md`。

2. **語料不在版控裡。** `sources/`、`vin-corpus*.txt`、`vin-raw-messages.jsonl`、
   `gold.jsonl` 這幾份只在本機，因為那是 Vin 本人的文件與私人訊息。
   換一台機器要重跑 `build_vin_corpus.py` 與 `extract_docs.py`。

3. **`~/.claude` 底下的掛勾指向這個 repo。** 在這裡改檔案，掛勾當場就吃到新版，
   所以改壞了會讓所有對話的回話被擋住。改完要跑一次 `README.md` 裡那幾個驗證指令。
