# 變更紀錄

## 2026-09-21　給 agent 讀的文件改用另一套規則

原本一份文件不管寫給誰讀，走的都是同一套檢查：這個說法 Vin 自己寫過嗎。
可是 agent 讀到自創詞不會卡，它照字面處理，也不會因為句子拗口就理解錯。
為了那個沒有人會付的成本去改稿，那一輪是白做的。它真正會做錯的是別的地方。

- 檔案開頭寫 `audience: agent`，掃詞就改用 `copy-samples/agent-doc-rules.json`。
  沒寫的一律當成人要讀 —— 漏掉一次的代價是他在畫面上讀到一句爛文案，比多掃一輪大。
- 只認開頭 400 個字裡的那一行，免得一份在講這個欄位的文件把自己整份放行。
  改既有檔案的時候掛勾只拿得到要換上去的那一段，所以會回頭讀那個檔的開頭。
- agent 那一套擋三種：條件寫得不明確（盡量、適當、視情況、原則上這一類，
  agent 會自己決定放寬到什麼程度）、同一個東西前後兩個名字、
  實測數字沒標明是參考值還是要追的目標。第一種擋人，後兩種提醒。
- 比對名稱前後是否一致的時候，程式碼區塊與行內的指令、檔名都先拿掉。
  內文寫「分支」而指令裡有 branch，那不是前後不一致。
  拿一份 15,000 字的真實規格試過：修掉之前跳三個，其中兩個是假的。

送出去給人審過一輪，修掉五件他抓到的事：

- **以檔案上的宣告為準，不是以這次要寫進去的那段字為準。** 不然只要在要送出去
  的文字最前面加一行，這段字就自己把自己放行了，而這一支存在的前提正是
  「自己讀自己剛寫的字，讀得懂就不覺得怪」。回話與交辦沒有檔案可讀，一律照人
  那一套。
- **讀不到規則檔的時候退回人那一套，不准印綠燈。** 原本的寫法會在規則檔多一個
  逗號之後，讓每一份 agent 文件都拿到一句「檢查通過」，而那時候人那一套也已經
  跳過了，等於整份沒有人看過。
- **讀檔案開頭改成限量讀，而且先確認是一般檔案。** 原本為了拿 400 個字會把整份
  讀進來：360 MB 的檔要 0.32 秒與 615 MB 記憶體，遇到具名管道直接停在那裡不
  回來 —— 這一支掛在每一個工具之前，停住等於整個對話卡住。
- **條件不明確的那幾個詞也照敘述比對。** 原本連程式碼區塊裡的字都擋，所以引用
  別人的規格、或是列出禁用詞的那一行，全部寫不出來。
- **英數的詞當成完整的字比對。** `app` 落在 `apply`、`application` 裡面，照子字串
  比會每一份規格都跳一次假的。

`tests/test_agent_audience.py` 把這幾條釘住，16 條每一條都真的跑一次掛勾看退出碼。

## 2026-09-15　把涵蓋範圍補到「預設全掃」

原本掃詞是名單制：程式先看這個工具在不在名單上，在名單上才掃。實測抓到三個漏掉的出口 ——
`gh issue comment -b` 的短旗標整段放行、`-F` 後面那個內文檔從來沒被讀過、
用 `gspread` 把工作日誌寫進 Google 試算表也沒人管。所以改成反過來做。

- 掛勾的比對規則改成 `.*`，每一個工具動之前都先掃，包含以後接的任何 MCP 工具。
  只掃會把內容帶出去的欄位，`file_path` 這種只是指路的欄位不掃。
- 對外指令的認法放寬：加上 Google 試算表、線上文件、聊天軟體、寄信，
  以及用 `cat`／`tee`／`echo` 把中文寫進專案檔案。拿最近 12 個對話的
  4,205 條帶中文指令重放過，多掃 15 條，其餘不變。
- 比對到是對外指令、卻讀不到要送出去的字，改成出聲警告，不再靜靜放行。
- `copy_judge.py` 與 `copy_gate_on_edit.py` 也掛到指令這一側，
  所以 issue 留言與提交訊息一樣要先跑過文案 skill、一樣會被讀一次。
- 畫面上的字（按鈕、空狀態、錯誤訊息）以前永遠到不了語意判官的 120 字門檻。
  改成先收起來，一輪結束再一起判一次，不用每改一句就等一次模型。
- `CHANGELOG` 不再跳過，`openspec/` 不再豁免。更版說明與規格都是寫給人讀的。
- 情境目錄加第 13 種：寫進線上試算表或線上文件的工作紀錄。
  讀者是 Vin、他同事、他主管。
- 認不出是哪一種情境、可是帶著中文的，會記一筆到 `unmatched.jsonl`，
  當成「還有哪幾種情境沒進目錄」的清單。

考題那一側也修了兩個標錯的地方：

- 「他沒抱怨」以前被當成「寫得好」，現在標成「不知道」，不進考試。
- 他說這段寫得好的時候，以前沒有程式在接。現在 `complaint_learn.py` 會把前一則
  存進 `approved.jsonl`，他認可過的段落從 4 段變成 10 段。
- 同一支程式以前把「他在討論聽不懂這三個字」當成他在抱怨，8 筆裡有 3 筆標錯，
  其中一筆是他在誇獎。現在會先把引號裡的字拿掉再判。

寫法規則多了五條，每一條都對著他 99.5 萬字統計過才設成一律攔下來：
數量的前後講反了、拿挖當資料處理的動詞、把機器的動作寫成對人做的動作、
拿呼叫對人用、把檢查程式叫成門。

## 2026-09-15　開 repo

從 `~/.claude` 底下把這套東西整理成一個獨立的 repo，`~/.claude` 那幾個位置改成指向這裡。

- 兩層檢查搬進 `hooks/`：掃詞的 `coined_word_guard.py`、讀意思的 `copy_judge.py`，
  另外三支各管編輯攔截、語料注入、抱怨學習。
- 規則與詞表搬進 `copy-rules/` 與 `copy-samples/`。
- 語料（42 萬字）與原始訊息紀錄不進版控，見 `.gitignore`。
- 公開之前把同事名字換成角色稱呼、拿掉內部規則編號、把客戶名稱與真實報告數字換成代稱。
- 名稱定為「文筆守門員 Tone Guard」。

已知還沒解決的：語意判官的判斷準確度，目前抓到 55%、誤判 27%，所以它只提醒不擋人。
